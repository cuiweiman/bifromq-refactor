package com.zachary.bifromq.dist.server.scheduler;

public enum SubCallType {
    ADD_TOPIC_FILTER,
    INSERT_MATCH_RECORD,
    JOIN_MATCH_GROUP,
    REMOVE_TOPIC_FILTER,
    DELETE_MATCH_RECORD,
    LEAVE_JOIN_GROUP,
    CLEAR
}
